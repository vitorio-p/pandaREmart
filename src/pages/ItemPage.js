// import React from "react";
// import { useParams, Link, generatePath, useHistory } from "react-router-dom";

// export default function ItemPage() {
//   const { id } = useParams();

//   const handleProceed = (e) => {
//     history.pushState(generatePath("/item/:id", { id }));
//   };
// }
